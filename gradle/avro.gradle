
generateAvroJava {
    source("src/resources/avro")
    outputDir = file("src/main/bio/terra/aws/resource/discovery/avro")
}


/*
def generateAvro = tasks.register("generateAvro", GenerateAvroJavaTask) {
    source = "src/resources/avro"
    outputDir = file("${buildDir}/avro")
}

 */

tasks.compileJava.dependsOn(generateAvroJava)

